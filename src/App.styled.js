import styled from 'styled-components';

export const ScWrapper = styled.main`
  margin: 0 auto;
  padding: 0 20px;
  max-width: 1240px;
`;

export const ScContainer = styled.div`
  display: flex;
  gap: 40px;
`;
