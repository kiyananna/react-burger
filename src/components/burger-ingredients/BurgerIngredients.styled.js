import styled from 'styled-components';

export const ScIngredients = styled.div`
  overflow-y: scroll;
  height: calc(100vh - 280px);
`;

export const ScTabs = styled.div`
  display: flex;
  margin-bottom: 10px;
`;
